# bundle (generated output — do not hand-edit)

This branch is populated entirely by `scripts/build-bundle.mjs` in the
superproject. Every file here is overwritten on each run. Edit the source
branches (`backend`, `frontend`, `cli`) and re-run the script instead.
